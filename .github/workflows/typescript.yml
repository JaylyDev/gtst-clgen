name: 'TypeScript Debug'
on: [push]

jobs:
  build:
    name: TypeScript Debug
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
        with:
          fetch-depth: 1
      - run: npm install
      - run: npm run test
      - run: npm run build
      - uses: ./types